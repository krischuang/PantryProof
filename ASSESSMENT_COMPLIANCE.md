# PantryProof - Assessment Compliance

This file maps every requirement of the Assessment 2 brief to concrete
evidence in this repository. Nothing below is marked PASS without a file,
test, or documented decision backing it.

| Requirement | Code Evidence | Test Evidence | Documentation Evidence | Status |
| --- | --- | --- | --- | --- |
| New project, not a copy of the old one | `git log --oneline` root commit `chore: initialise FridgeFix iOS project` (the repository's original working name, since refined to PantryProof), built from the Xcode SwiftUI template, no old-project files imported | - | README setup instructions | PASS |
| Domain-centred architecture (Views → ViewModels → Use Cases → Domain/Services → Data) | `PantryProof/{Views,ViewModels,UseCases,Domain,Services,Data}` folder structure | - | README "Architecture" section; `docs/ARCHITECTURE_DIAGRAM.md` | PASS |
| ≥3 genuine Use Cases | `EvaluateRecipeFeasibilityUseCase.swift`, `AddPantryItemUseCase.swift`, `AddMissingIngredientToShoppingListUseCase.swift` (required three); `UpdatePantryItemUseCase.swift`, `RemovePantryItemUseCase.swift`, `ToggleShoppingListItemUseCase.swift`, `RemoveShoppingListItemUseCase.swift` (justified optional additions - see "Every Use Case has a typed domain error" below) | 14 + 6 + 3 + 2 + 6 + 3 + 2 = 36 use case tests | README "Use Cases" table | PASS |
| Every Use Case has a typed domain error, happy-path test, and failure-path test | See the Use Case → Error → Tests table below | See table below | README "Use Cases" table; README "Error and Recovery Design" | PASS |
| Domain models use semantic, non-vague names | `Ingredient`, `PantryItem`, `Recipe`, `RecipeIngredient`, `IngredientRole`, `IngredientAvailability`, `PantrySubstitute`, `RecipeEvaluation`, `RecipeFeasibility`, `ShoppingListItem` - no `Item`/`Manager`/`Handler` types | - | DocC comments on every model (`PantryProof.docc/PantryProof.md` + inline `///`) | PASS |
| `struct` for domain records | All 10 domain model files are `struct`; only repositories/services (`InMemoryPantryRepository`, `InMemoryShoppingListRepository`, `InMemoryRecipeRepository`, `LocalSubstitutionService`) and ViewModels are reference types, each for a stated reason (shared mutable storage; SwiftUI observation) | - | README architecture note; inline doc comments | PASS |
| `IngredientRole` (essential/replaceable/optional) | `Domain/Models/RecipeIngredient.swift` | `RecipeEvaluationTests.test_feasibility_ignoresOptionalIngredients_*` | README "Business Rules" | PASS |
| `IngredientAvailability` (available/insufficient/missing/quantityUnverified) | `Domain/Models/IngredientAvailability.swift` | `EvaluateRecipeFeasibilityUseCaseTests` (exact/greater/lower quantity, missing, differing units) | README "Business Rules" | PASS |
| No unsafe unit mismatch presented as available | `EvaluateRecipeFeasibilityUseCase.availability(of:comparedTo:)` reports `.quantityUnverified` (never `.available`) when pantry/recipe units differ; `RecipeEvaluation.feasibility` never returns `.readyToCook` while an essential/replaceable ingredient is `.quantityUnverified` | `test_evaluateRecipe_marksQuantityUnverified_whenPantryAndRecipeUnitsDiffer`, `test_evaluateRecipe_doesNotReportReadyToCook_whenEssentialQuantityCannotBeVerified`, `RecipeEvaluationTests.test_feasibility_isCanMakeWithAdjustments_whenEssentialIngredientQuantityIsUnverified`, `test_feasibility_ignoresOptionalIngredients_whenQuantityIsUnverified` | README "Business Rules"; `PantryProof.docc/PantryProof.md` "Availability is four states, not two" | PASS |
| `RecipeFeasibility` (readyToCook/canMakeWithAdjustments/blocked) | `Domain/Models/RecipeFeasibility.swift` | `RecipeEvaluationTests`, `EvaluateRecipeFeasibilityUseCaseTests` | README "Business Rules" | PASS |
| Recipe feasibility rules match brief exactly, no contradictions | `Domain/Models/RecipeEvaluation.swift` (`feasibility` computed property - single source of truth) | `test_feasibility_prefersBlocked_whenBothUnresolvedAndAdjustableIngredientsExist` | `docs/REFLECTIVE_REPORT.md` "Domain understanding" | PASS |
| Typed domain errors with human-facing messages | `AddPantryItemError`, `UpdatePantryItemError`, `RemovePantryItemError`, `ToggleShoppingListItemError`, `RemoveShoppingListItemError`, `AddMissingIngredientToShoppingListError`, `EvaluateRecipeFeasibilityError` (all `LocalizedError`) | See Use Case → Error → Tests table below | README "Error and Recovery Design" | PASS |
| Errors explain what happened + next action | e.g. `AddPantryItemError.duplicateIngredient` message; `UpdatePantryItemView` reachable directly from that error; quantity-unverified rows show the pantry's own amount plus "check the amount before cooking" | `test_pantryViewModel_addItem_exposesDuplicateItem_forGuidedRecovery` | README error table; reflective report | PASS |
| ≥4 functional SwiftUI screens | `HomeView`, `PantryView`, `RecipeListView`, `RecipeDetailView`, `ShoppingListView` (5, plus supporting forms `AddPantryItemView`/`UpdatePantryItemView`) | Verified via `xcodebuild build` + Simulator launch/screenshot | README "Project Structure" | PASS |
| RecipeDetailView shows role, availability, substitutions, verdict | `Views/Recipes/RecipeDetailView.swift` | Backed by `EvaluateRecipeFeasibilityUseCaseTests` (data) | README "Core Features" | PASS |
| Add missing ingredients to Shopping List from Recipe Detail | `IngredientEvaluationRow.onAddToShoppingList` → `ShoppingListViewModel.addMissingIngredient` | `AddMissingIngredientToShoppingListUseCaseTests` | README "Core Features" | PASS |
| ≥8 meaningful tests, happy + failure + boundary paths | 59 tests total across `PantryProofTests/{UseCases,Domain,ViewModels}` | `xcodebuild test` - 59/59 passing | README "Testing" | PASS |
| Test names describe business behaviour | e.g. `test_evaluateRecipe_blocksCooking_whenEssentialIngredientIsMissingWithNoSubstitute` | All test files | - | PASS |
| Views contain no significant business logic | Views only read ViewModel state and call ViewModel methods; all validation/feasibility/duplicate/guidance-mapping logic lives in `UseCases`/`Domain`/`ViewModels` - `RecipeDetailView`'s `FeasibilityBanner` renders a pre-computed `RecipeFeasibility` + guidance string from `RecipeViewModel.feasibilityGuidance` rather than filtering `RecipeEvaluation` itself | - | README architecture section | PASS |
| Every significant mutation goes through a Use Case | `PantryViewModel.removeItem` → `RemovePantryItemUseCase`; `ShoppingListViewModel.removeItem` → `RemoveShoppingListItemUseCase` (previously called the repository directly, bypassing the Use Case layer) | `RemovePantryItemUseCaseTests`, `RemoveShoppingListItemUseCaseTests`, `PantryViewModelTests.test_pantryViewModel_removeItem_*`, `ShoppingListViewModelTests.test_shoppingListViewModel_removeItem_*` | README "Use Cases" table | PASS |
| Meaningful DocC on models and use cases | `///` doc comments on every type in `Domain/Models`, `Domain/Errors`, `Domain/Protocols`, `UseCases`; `PantryProof.docc/PantryProof.md` landing article (verified with `xcodebuild docbuild`) | - | - | PASS |
| Complete README | `README.md` - all 12 required sections present | - | - | PASS |
| Human-System Architecture Diagram | `docs/ARCHITECTURE_DIAGRAM.md` + `docs/layer-diagram.png` + `docs/human-system-diagram.png` | - | - | PASS |
| 600–800 word reflective report | `docs/REFLECTIVE_REPORT.md` (~786 words) | - | - | PASS |
| Professional Git history, feature branches, Conventional Commits | 8 feature/docs branches, each merged with `--no-ff`; every commit prefixed `feat:`/`fix:`/`test:`/`docs:`/`chore:` | `git log --oneline --graph` | this file | PASS |
| Incremental commits and pushes, no giant final commit | Largest single commit is the Phase 5 `RecipeDetailView` add (~154 lines); every phase pushed after each commit | `git log` per branch | - | PASS |
| Stable main branch | `main` only receives `--no-ff` merges after build + full test pass on the feature branch | `git log main --oneline --graph` | - | PASS |

## Use Case → Typed Error → Test coverage

| Use Case | Source File | Typed Error | Happy-Path Test | Failure-Path Test |
| --- | --- | --- | --- | --- |
| `EvaluateRecipeFeasibilityUseCase` | `UseCases/EvaluateRecipeFeasibilityUseCase.swift` | `EvaluateRecipeFeasibilityError.recipeHasNoIngredients` | `test_evaluateRecipe_isReadyToCook_whenAllIngredientsAreFullyAvailable` (+ 12 more) | `test_evaluateRecipe_fails_whenRecipeContainsNoIngredients` |
| `AddPantryItemUseCase` | `UseCases/AddPantryItemUseCase.swift` | `AddPantryItemError` (`emptyIngredientName`, `invalidQuantity`, `duplicateIngredient`) | `test_addPantryItem_succeeds_withValidNameAndQuantity` | `test_addPantryItem_fails_whenIngredientNameIsEmpty`, `test_addPantryItem_fails_whenQuantityIsNegative`, `test_addPantryItem_fails_whenIngredientAlreadyExists` |
| `UpdatePantryItemUseCase` | `UseCases/UpdatePantryItemUseCase.swift` | `AddPantryItemError.invalidQuantity` / `UpdatePantryItemError.itemNotFound` | `test_updatePantryItem_succeeds_withValidQuantity` | `test_updatePantryItem_fails_whenQuantityIsNegative`, `test_updatePantryItem_fails_whenItemNoLongerExists` |
| `RemovePantryItemUseCase` | `UseCases/RemovePantryItemUseCase.swift` | `RemovePantryItemError.pantryItemNotFound` | `test_removePantryItem_succeeds_whenItemExists` | `test_removePantryItem_fails_whenItemNoLongerExists` |
| `AddMissingIngredientToShoppingListUseCase` | `UseCases/AddMissingIngredientToShoppingListUseCase.swift` | `AddMissingIngredientToShoppingListError.invalidRequiredQuantity` | `test_addMissingIngredient_addsNewItem_whenNotAlreadyOnList` (+ 2 more) | `test_addMissingIngredient_fails_whenRequiredQuantityIsZero`, `test_addMissingIngredient_fails_whenRequiredQuantityIsNegative` |
| `ToggleShoppingListItemUseCase` | `UseCases/ToggleShoppingListItemUseCase.swift` | `ToggleShoppingListItemError.itemNotFound` | `test_toggleItem_marksIncompleteItemAsCompleted`, `test_toggleItem_marksCompletedItemAsIncomplete` | `test_toggleItem_fails_whenItemNotFound` |
| `RemoveShoppingListItemUseCase` | `UseCases/RemoveShoppingListItemUseCase.swift` | `RemoveShoppingListItemError.itemNotFound` | `test_removeShoppingListItem_succeeds_whenItemExists` | `test_removeShoppingListItem_fails_whenItemNoLongerExists` |

All 7 use cases satisfy "typed domain error + happy-path test + failure-path test." None of the failure cases above are synthetic - each names a genuine invalid business state (see README "Use Cases" and "Business Rules").

## Known, disclosed limitations

- **UI walkthrough beyond the Home screen was verified by build success and full ViewModel/UseCase test coverage, not by interactive on-device screenshots of every screen.** The sandboxed CI-like environment this was built in has no macOS Accessibility permission for automated tap simulation (`osascript`/System Events), and adding a dedicated XCUITest target was judged out of scope for this assessment. The Home screen was confirmed visually with live wired data (see session screenshot); Pantry, Recipe List, Recipe Detail, and Shopping List all build successfully and are backed by fully unit-tested ViewModels/UseCases exercising the same logic each screen renders.
- **No persistence across app launches.** Deliberate MVP scope (see `docs/REFLECTIVE_REPORT.md` "What I would do next") - every repository is in-memory, so the pantry and shopping list reset each run. This keeps demos reproducible but is the most realistic next step.
- **No unit conversion between measurement units.** Deliberate, documented scope limitation (see README "Business Rules") rather than an oversight - an incorrect conversion was judged worse than an honest fallback. Differing units are reported as `quantityUnverified`, never as `available`, so this limitation is now surfaced honestly rather than silently masked.
- **Deployment target (`IPHONEOS_DEPLOYMENT_TARGET = 26.1`) left unchanged.** Verified against the actual build environment: `xcodebuild -version` reports Xcode 26.1, and `xcodebuild -showsdks` lists iOS 26.1 as the only installed iOS/Simulator SDK, so 26.1 is not an arbitrarily high target - it is the only version this project can currently build and test against. README "Setup Instructions" already documents "Xcode 26 or later" / "iOS Simulator (iOS 26.1+)" consistently with this.
